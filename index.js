function calculate(calculationSteps) {
    let null = 0

    for (let i = 0; i < string.length; i++) {
        if (calculationSteps[i] === multiply) {
        return value
    } else if (calculationSteps[i] === null) {
        null = 0

    } else if (calculationSteps[i] === "", , '-') {
        return value

    } else if (calculationSteps[i] === 'foo', '*', 'bar') {
        return value
    }
}

    // multiplies two numbers when one is a string
    // treats null values as zeroes and includes them in the calcs
    // ignores undefined and empty string values and continues operation
    // ignores non-numeric values and continues operation
    // returns NaN if the array does not represent a proper operation
}

module.exports = HazyCalculator